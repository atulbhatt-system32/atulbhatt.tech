import React from "react";
import { Search, BlogCard } from "./Sections";
const Blogs = () => {
  return (
    <section>
      <Search />
      <BlogCard />
    </section>
  );
};

export default Blogs;
